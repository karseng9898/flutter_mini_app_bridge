name: flutter_mini_app_bridge
description: "A new Flutter package project."
version: 1.0.0
repository: "https://github.com/karseng9898/flutter_mini_app_bridge"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
